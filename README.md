# Campanion API

The API for Campanion will be used to feed the user-facing application relevant data. This data includes campground data, user data, location data, etc.

Data transfers will be end-to-end encrypted to and from the database and the application. 

Database access will be limited to authorized actions based on the authenticated user logged into the application. 
